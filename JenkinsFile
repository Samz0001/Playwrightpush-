pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                echo "📦 Checking out code from Git..."
                git branch: 'main', url: 'https://github.com/Samz0001/Playwrightpush-.git'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    echo "🐳 Building Playwright Docker image..."
                    bat '''
                        if exist Dockerfile (
                            docker build -t playwright-tests .
                        ) else (
                            echo "❌ Dockerfile not found in workspace root!"
                            exit /b 1
                        )
                    '''
                }
            }
        }

        stage('Run Playwright Tests in Docker') {
            steps {
                script {
                    echo "🧪 Running Playwright tests inside Docker container..."
                    bat """
                        docker run --rm ^
                        --ipc=host ^
                        -e CI=true ^
                        -v "%WORKSPACE%":/app ^
                        -w /app ^
                        playwright-tests /bin/bash -c "npm ci && npx playwright test google.spec.js --reporter=html || exit 0"
                    """
                }
            }
        }
    }

    post {
        always {
            echo "📊 Publishing Playwright HTML report inside Jenkins..."
            publishHTML(target: [
                allowMissing: false,
                alwaysLinkToLastBuild: true,
                keepAll: true,
                reportDir: 'playwright-report',
                reportFiles: 'index.html',
                reportName: 'Playwright Test Report',
                reportTitles: 'Playwright Execution Summary'
            ])

            echo "📦 Archiving Playwright report for download..."
            archiveArtifacts artifacts: 'playwright-report/**', fingerprint: true

            echo "🧹 Cleaning workspace but keeping archived artifacts..."
            script {
                try {
                    cleanWs(
                        notFailBuild: true,
                        deleteDirs: true,
                        patterns: [[pattern: 'playwright-report/**', type: 'EXCLUDE']]
                    )
                } catch (Exception e) {
                    echo "⚠️ Warning: cleanup failed, continuing..."
                }
            }
        }
    }
}
