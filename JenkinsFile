pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                echo "📦 Checking out code from Git..."
                git branch: 'main', url: 'https://github.com/Samz0001/Playwrightpush-.git'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    echo "🐳 Building Playwright Docker image..."
                    bat '''
                        if exist Dockerfile (
                            docker build -t playwright-tests .
                        ) else (
                            echo "❌ Dockerfile not found in workspace root!"
                            exit /b 1
                        )
                    '''
                }
            }
        }

        stage('Run Playwright Tests in Docker') {
            steps {
                script {
                    echo "🧪 Running Playwright tests inside Docker container..."
                    bat """
                        docker run --rm ^
                        --ipc=host ^
                        -e CI=true ^
                        -v "%WORKSPACE%":/app ^
                        -w /app ^
                        playwright-tests /bin/bash -c "npm ci && npx playwright test --reporter=html"
                    """
                }
            }
        }
    }

    post {
        always {
            echo "📊 --- Publishing Playwright Reports ---"

            // Publish Playwright HTML Report (Requires HTML Publisher Plugin)
            publishHTML(target: [
                reportDir: 'playwright-report',
                reportFiles: 'index.html',
                reportName: 'Playwright Test Report',
                keepAll: true,
                alwaysLinkToLastBuild: true,
                allowMissing: true
            ])

            echo "🧹 Cleaning up workspace..."
            script {
                try {
                    cleanWs()
                } catch (Exception e) {
                    echo "⚠️ Warning: workspace cleanup failed, ignoring..."
                }
            }
        }
    }
}
